# Library

My own implementations of standard algorithms and data structures, each tested on a judge problem before it goes in: DSU, Fenwick tree, segment tree, sparse table, LCA, Dijkstra, modular arithmetic, string hashing, and more over time.
